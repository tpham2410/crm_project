
spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/crm_app"
    username: "root"
    password: "1234"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8082
  servlet:
    context-path: /api

logging:
  level:
    org:
      springframework:
        web=DEBUG:

